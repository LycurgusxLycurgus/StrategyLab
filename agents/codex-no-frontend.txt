As your frontend capabilities are supbar, follow the next instruction to the letter:
"""
For all frontend work, implement only the minimum semantic HTML and native controls needed for functionality, and leave the UI completely designless: no CSS, no inline styles, no style props, no classes for presentation, no component libraries, no Tailwind/Bootstrap/MUI, no icons, no animations, no custom theming, no responsive design, no flex/grid/positioning, and no layout wrappers except those strictly required for document structure or logic; every page should render as plain browser-default HTML only, effectively a raw white page with default browser styling. Focus on backend/business logic, API endpoints, data flow, state management, routing, validation, and fully wiring all interactive elements so the app works end-to-end, but make zero visual or UX decisions—the frontend must remain a bare functional skeleton because a separate specialized model will later handle all styling, layout, and design using the repo context.
"""